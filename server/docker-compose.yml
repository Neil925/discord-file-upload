# Generated by professor GPT cause I can't be bothered.
version: '3.8'  # Specify the version of the Docker Compose file format

services:
  mongodb:
    image: mongo:latest  # Use the latest MongoDB image
    container_name: mongodb_container  # Name of the container
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin  # Root username
      MONGO_INITDB_ROOT_PASSWORD: zu3TFriFAEfBYF6D # Root password
      MONGO_INITDB_DATABASE: discord_upload_db # Default database to create
    ports:
      - "27017:27017"  # Map the MongoDB port to the host
    volumes:
      - mongodb_data:/data/db  # Persistent data storage for MongoDB

volumes:
  mongodb_data:  # Define a named volume for MongoDB data
